Para executar o servidor, basta abrir um terminal (no Linux), cmd (no Windows) ou o IDLE de python no diretório raiz e executar o arquivo "WebServer.py" utilizando python2. A porta padrão configurada no servidor é a 6789.

# Desenvolvido por Matheus Costa e William Gomes




To run the server, open a Linux Terminal, Windows cmd or a python IDLE on the root directory and run the file "WebServer.py" using python2. The default server port is 6789.

# Developed by Matheus Costa and William Gomes
